Criterion is a great tool!
Show is an arse, perf between list and list of partitions is very slow for some reason...your prints will later your benchmark numbers.

ghc -XRankNTypes -XScopedTypeVariables -XGADTs -O2 -fllvm -funfolding-use-threshold=16 ./disc_sort.hs -rtsopts
